import { AuthData } from '../interfaces/interfaces';

const currentUser: AuthData = {
    message: '',
    token: '',
    refreshToken: '',
    userId: '',
    name: '',
};

export default currentUser;
